# 260702-5 更新說明

本版針對準備清單介面一致性調整：

1. 分類新增改成單一輸入框，可直接輸入「🪪 證件」。
2. 分類編輯也改成單一欄位，不再把 emoji 獨立成另一個輸入。
3. 分類與項目的編輯 / 刪除改成縮小版行程卡片按鈕樣式：✏️ / ✕。
4. 保留 260702-4 的局部更新邏輯，勾選與新增項目不會整個重畫面板。
5. 後端 API URL 維持新版 Apps Script。

更新檔案：

- index.html
- config.js
- prep-checklist.js
- README_260702-5.md

部署：

```powershell
git pull --rebase origin main
git add index.html config.js prep-checklist.js README_260702-5.md
git commit -m "260702-5 Refine checklist controls"
git push origin main
```

測試網址：

```text
https://kim-ych.github.io/Travel/?v=260702-5
```
