(function (window) {
  'use strict';

  // Reserved for itinerary helpers. Image handling lives in places.js because
  // Google Places PhotoService URLs are temporary and need careful handling.
  window.TravelItinerary = window.TravelItinerary || {};
})(window);
